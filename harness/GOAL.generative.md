# GOAL (generative-mode demo seed)

**Statement:** Does every real-analytic F with a fixed point of multiplier λ (|λ|≠0,1) admit a real-analytic half-iterate (functional square root) on a neighborhood — and when is it unique?

**Mode hint:** generative

> A terse hard statement in the project's own domain. Used by the generative dry-run (plan §7).
> The harness must NOT return "solved/unsolved" — it ships the **geometry of this problem's location**
> in the knowledge field (nearest verified landmarks, gap shape, bridges). IRON RULE: no node is
> ever stamped `unsolved`/`impossible`.
